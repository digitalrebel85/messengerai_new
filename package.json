{
  "name": "express-bootstrap-sqlite",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "build": "mkdir -p dist && cp -r src/views dist/ && cp -r src/public dist/ && cp src/server.js dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "ejs": "^3.1.9",
    "bootstrap": "^5.3.2",
    "express-session": "^1.17.3",
    "express-validator": "^7.0.1",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "twilio": "^4.19.0",
    "connect-flash": "^0.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0"
  }
}